package org.itstep.zoo;

public abstract class Dog extends Animal {
    public Dog(String nickname, int age, double weigth, int number, int time) {
        super(nickname, age, weigth, number, time);
    }
}
